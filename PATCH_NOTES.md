# 최근 업데이트 패치 노트 (Patch Notes)

## 🚀 새로운 기능 (Features)
- **패치노트 실시간 연동 및 캐싱 도입**
  - 백엔드(`/api/patch-notes`)에서 GitHub API를 호출하여 메모리에 24시간 동안 캐시하도록 구현했습니다.
  - 이를 통해 시간당 60회로 제한된 GitHub 기본 Rate Limit에 구애받지 않고 무제한 조회가 가능해졌습니다.
  - 당일 업로드된 신규 패치노트의 경우, 자동으로 파란색 **NEW** 뱃지를 달아주어 업데이트 내역을 놓치지 않게 개선했습니다.

- **패치노트 페이지네이션 (Pagination) 추가**
  - 역대 업데이트 내역을 모두 볼 수 있도록 백엔드의 커밋 조회 한도를 100개로 상향했습니다.
  - 프론트엔드 전체 보기 페이지(`PatchNotesPage.tsx`) 하단에 페이지네이션(이전/다음) 버튼을 도입하여 한 페이지에 10개씩 쾌적하게 열람 가능합니다.

## 🎨 UI/UX 개선 (Style)
- **우측 하단 사이드바 패치노트 토글 기능**
  - 우측 하단에 떠있는 미니 패치노트 창 상단에 **[숨기기]** 버튼을 추가했습니다.
  - 숨김 처리 시 자리 차지를 최소화하는 원형 로켓/문서 아이콘 형태로 최소화되며, `localStorage`에 상태가 저장되어 새로고침 후에도 유지됩니다.
  - 로켓 이모지(🚀)를 흑백 테마 및 다크 모드에 완벽하게 어우러지는 세련된 SVG 아이콘(문서 형태)으로 교체하여 디자인 일체감을 대폭 향상시켰습니다.
  - 최소화 상태에서 오늘 신규 패치노트가 존재할 경우, 우측 상단에 파란색 Ping(깜빡임) 효과가 렌더링되어 사용자가 즉각적으로 인지할 수 있도록 디테일을 더했습니다.
