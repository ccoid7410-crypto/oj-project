# 문제 유형과 채점 규칙

## 일반형 (`STANDARD`)

기존 채점 방식이다. 테스트케이스 입력을 프로그램의 표준 입력으로 전달하고, 줄 끝 공백과 마지막
개행을 정규화한 뒤 기대 출력과 비교한다.

## 정확도/최적화형 (`SCORING`)

프로그램 출력과 테스트케이스 기대 출력의 **첫 번째 토큰**을 유한한 숫자로 해석한다. 테스트별
배점은 `maxScore / 테스트 수`이고, 다음 품질 비율(0~1)을 곱해 합산한다.

- `TARGET`: `1 - |출력 - 기준값| / max(|기준값|, 1e-9)`
- `MAXIMIZE`: `출력 / 기준값`
- `MINIMIZE`: `기준값 / 출력`

비율은 0~1로 제한한다. 최대화/최소화 기준값은 0보다 커야 하며, 최소화 출력도 0보다 커야 한다.
숫자로 해석할 수 없는 출력은 `WRONG_ANSWER`다. 실행 가능한 숫자 출력을 모든 테스트에 내면
`ACCEPTED`와 함께 제출·테스트별 점수가 저장된다.

## 인터랙티브형 (`INTERACTIVE`)

현재 구현은 임의 코드를 가진 커스텀 인터랙터가 아니라 **결정적 줄 단위 대화** 방식이다.

- 테스트케이스 `input`의 각 줄: 채점기가 프로그램에 보낼 요청
- 같은 위치의 `output` 줄: 프로그램에서 기대하는 응답
- 요청 한 줄을 보낸 뒤 응답 한 줄을 기다리고 비교한 다음 다음 요청을 보낸다.

입력과 출력 줄 수는 같아야 한다. 프로그램은 매 응답 뒤 표준 출력을 flush해야 한다. 전체 대화에
문제 시간 제한이 적용된다. 응답 불일치는 `WRONG_ANSWER`, 조기 종료/프로토콜 오류는
`RUNTIME_ERROR`, 무응답은 `TIME_LIMIT_EXCEEDED`다.

응답에 따라 다음 질문이나 정답이 동적으로 달라지는 완전한 커스텀 인터랙터는 아직 지원하지 않는다.

## 문제별 컴파일 옵션

출제 화면에서 C, C++, Java, Go의 추가 컴파일 인자를 언어별로 지정한다. 각 줄은 하나의 argv
인자이며, 전역 `JudgeConfig`로 결정된 컴파일 명령의 소스 파일 인자 바로 앞에 삽입된다.
Python과 JavaScript는 별도 컴파일 단계가 없어 문제별 컴파일 옵션을 받을 수 없다.

## 연습 문제

`isPractice` 문제는 일반 공개 문제와 동일하게 제출·채점·풀이 상태를 기록하지만 사용자 레이팅
계산에서는 제외한다. 문제 목록의 “연습 문제” 탭에서 따로 볼 수 있다.
